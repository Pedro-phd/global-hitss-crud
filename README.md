# CRUD Lista de Produtos

Olá, seja bem-vindo ao teste.

Para testar é necessário o uso do json-server

`npm install -g json-server`

Para seu funcionamento, vamos usar o seguinte comando:

`json-server --watch mock.json`
